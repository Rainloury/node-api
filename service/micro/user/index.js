'use strict';

const moment = require('moment');
const {ajax} = require('../../utils/request');
const Respond = new require('../../utils/respond')();
const { ERRORS} = require('../../constant');

module.exports = function() {
  this.add('role: dictservice, cmd: dict, opt: get', async (data, respond) => {
     
  })
}